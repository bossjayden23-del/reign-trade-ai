# AI engine README

This folder contains the AI engine scaffold. No models or trading logic are implemented yet.

Quick run (inside devcontainer):

source .venv/bin/activate
python -m ai_engine.cli --version
