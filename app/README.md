# App README

This directory contains a minimal Flutter app. To run locally inside the devcontainer:

cd app
flutter pub get
flutter run

If platform directories (android/, ios/) are missing, run:

flutter create .
